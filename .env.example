# LLM is invoked via the `codex` CLI (npm install -g @openai/codex && codex login).
# No API key needed — codex uses your ChatGPT login.
DD_MODEL=gpt-5.5
DD_MODEL_FAST=gpt-5.5-mini
DD_CODEX_BIN=codex

# --- Web search backends (any one is enough; cascade falls through in this order) ---
# Perplexity is recommended: search + grounded synthesis in one call.
PERPLEXITY_API_KEY=pplx-...
DD_PPLX_MODEL=sonar
# Gemini with Google Search grounding — equivalent quality to Perplexity.
GEMINI_API_KEY=AIza...
DD_GEMINI_MODEL=gemini-2.5-flash
# Tavily — classic search index, URL list only.
TAVILY_API_KEY=tvly-...
# (no env var for DuckDuckGo — used automatically as last-resort fallback)

# --- notice.co secondary-market scraper ---
# Pull via `python scripts/refresh_notice_cookie.py` from your logged-in browser,
# or copy-paste the cookie string from Safari Web Inspector here:
NOTICE_CO_COOKIE=

SEC_EDGAR_USER_AGENT="DD Agent contact@example.com"
DD_DATA_DIR=./data
DD_DB_PATH=./data/deals.db
DD_ENABLE_PHOTO_CLASSIFIER=1
DD_LOG_LEVEL=INFO
